# ===========================================
# Notion API Configuration
# ===========================================
# 이 파일을 .env.local로 복사한 후 실제 값을 입력하세요.
# .env.local은 Git에 커밋되지 않습니다.

# Notion Integration Token
# 발급 방법: https://www.notion.so/my-integrations 에서 새 Integration 생성
# 형식: secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTION_API_KEY=secret_your_integration_token

# ===========================================
# Notion Database IDs
# ===========================================
# 데이터베이스 ID 확인 방법:
# 1. Notion에서 데이터베이스 페이지 열기
# 2. Share > Copy link 클릭
# 3. URL에서 ID 추출: https://notion.so/{workspace}/{DATABASE_ID}?v=...
# 4. DATABASE_ID 부분이 32자리 ID (하이픈 없이)

# Portfolio (종목 마스터) 데이터베이스 ID
# 필수 속성: Name(title), Ticker(rich_text), Sector(rich_text), DividendFrequency(select)
NOTION_PORTFOLIO_DATABASE_ID=your_database_id_here

# ExchangeRate (월별 환율) 데이터베이스 ID
# 필수 속성: Month(title, YYYY-MM 형식), Rate(number), BaseDate(date)
NOTION_EXCHANGE_RATE_DATABASE_ID=your_database_id_here

# DividendLog (배당 내역) 데이터베이스 ID
# 필수 속성: PaymentDate(date), Amount(number), Portfolio(relation), ExchangeRate(relation)
NOTION_DIVIDEND_LOG_DATABASE_ID=your_database_id_here
